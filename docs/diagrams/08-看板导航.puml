@startuml 08-看板导航
' covhub 2.1 —— Vue 看板的页面层级与每页背后的接口
' 对应代码：web/src/router.ts、web/src/pages/*.vue、covhub/views.py、covhub/api/routes_view.py
' 层级是 项目 → 服务 → 详情；hash 路由（避开 /<svc>/versions/... 这些静态报告路径）。

title 看板导航 · 页面层级与数据接口

skinparam shadowing false
skinparam NoteFontSize 11
skinparam defaultTextAlignment left
skinparam rectangle {
  BackgroundColor #F4F6F5
  BorderColor #8A9A98
  FontSize 12
}
skinparam ArrowFontSize 11

rectangle "侧栏（全站）\n项目下拉 · 项目总览 · <项目> 的服务 · 项目报表 · 未分组服务\n主题 浅 / 深 / 自动 · 接口文档 (Swagger)" as side

rectangle "项目总览  #/\n项目卡片：服务数、在线 / 离线、每个服务的\n运行时总 / 新增、单测总 / 新增\n新建项目\n<b>GET /api/overview</b>  GET /api/projects" as home

rectangle "项目面板  #/projects/<项目>\n服务表：状态、版本、运行时总 / 新增、单测总 / 新增、触达类、最后采集\n添加服务（勾选即生效）· 移出 · 编辑 / 删除项目\n<b>GET /api/overview</b>  PATCH /api/services/{name} {project}" as proj

rectangle "未分组服务  #/unassigned\n已登记但没归入项目的服务，可直接选项目归入" as pool

rectangle "项目报表  #/projects/<项目>/report?days=30\nKPI · 各服务横条图 · 服务现状表\n期间已结算版本表 · 期间单测报告表\n导出 CSV · 导出 PDF · 打印\n<b>GET /api/projects/{name}/report?days=</b>" as report

rectangle "服务详情  #/services/<服务>?v=<归档>&tab=<页签>\n环形指标 ×4（运行时 / 单测 × 总 / 新增）\n版本下拉：当前周期 | 任一归档（历史版本）\n立即采集 · 结算归档 · 重出报告 · 导出 PDF 报告\n<b>GET /api/services/{name}/detail[?version=]</b>\nPOST /api/dump · /api/predeploy · /api/report" as svc

rectangle "概览\n趋势图（总 / 新增两条线）· 数据来源" as t1
rectangle "新增代码\n按文件明细，展开看源码逐行执行状态\n<b>GET .../source?file&kind&version</b>" as t2
rectangle "已结算版本\n归档列表 → 在看板里看 / HTML / jacoco.xml" as t3
rectangle "历史对比\n基准 A → 对比 B：总量差 + 按文件指令覆盖差\n<b>GET .../compare?a&b</b>" as t4
rectangle "单测覆盖率\n单测报告历史" as t5
rectangle "配置\nincludes / excludes / classfiles / sourcefiles…" as t6

rectangle "JaCoCo 原生报告\n/<服务>/current/html/  /<服务>/versions/<v>/html/\n（dataDir 静态托管，受令牌门禁）" as jacoco

side -right-> home
home --> proj : 点项目卡片
home --> pool : 未分组
proj --> svc : 点服务
pool --> svc
proj --> report : 报表
svc --> t1
svc --> t2
svc --> t3
svc --> t4
svc --> t5
svc --> t6
t3 ..> svc : 切到历史版本 ?v=
t2 ..> jacoco : 在报告里看整个文件
svc ..> jacoco : JaCoCo 报告
report ..> svc : 点服务 / 版本

note bottom of svc
  **鉴权**：看板产物本身免令牌，/api/* 与 dataDir 要令牌；
  SPA 拿到 401 弹令牌输入，跳 /?token= 种 Cookie 后 302 回来。
  **颜色规则**：只有两个系列色（总覆盖蓝、新增代码橙），
  覆盖率数字不按阈值着色，语义色只给运维状态。
  在线状态由采集线程写库，页面不做 TCP 探活。
end note

@enduml
